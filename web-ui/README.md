# Web UI for Autoscaler

This is a React-based dashboard for monitoring and controlling the autoscaler.

## Installation

Run `npm install` to install dependencies.

## Running the Application

Use `npm start` to run the application.
